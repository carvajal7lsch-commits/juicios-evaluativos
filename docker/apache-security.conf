# Directivas de ámbito de servidor.
#
# ServerTokens solo es válida en la configuración global: dentro de un
# <VirtualHost> Apache aborta el arranque con "ServerTokens not allowed here".
# Por eso vive aquí y no en apache-vhost.conf.

ServerName localhost
ServerTokens Prod
ServerSignature Off
